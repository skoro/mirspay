nelmio_api_doc:
    documentation:
        info:
            title: Payment Proxy Service
            description: A proxy providing an api for payment your site orders via various payment gateways.
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
